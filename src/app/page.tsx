import { Login } from "@/components/login";

export default function Home() {
  return (
    <main className="w-full h-screen flex flex-col bg-purple-500 items-center justify-center">
      <Login />
    </main>
  );
}
